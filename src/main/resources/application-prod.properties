# =============================================== #
# == CONFIGURAÇÕES DE PRODUÇÃO (PostgreSQL)    == #
# =============================================== #

# --- CONFIGURAÇÃO DO BANCO DE DADOS (PostgreSQL/Neon) ---
spring.datasource.url=jdbc:postgresql://ep-fragrant-darkness-acusmxeg-pooler.sa-east-1.aws.neon.tech/database
spring.datasource.username=neondb_owner
spring.datasource.password=npg_FIleSfs97PQb
spring.datasource.driver-class-name=org.postgresql.Driver

# --- CONFIGURAÇÃO DO JPA/HIBERNATE ---
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false 
# Desligado em produção para não poluir os logs

# --- CONFIGURAÇÕES DE SEGURANÇA (JWT) ---
# Use o mesmo valor que está no seu application.properties principal
# api.security.token.secret=${JWT_SECRET}

# --- CONFIGURAÇÕES DE IA (OpenAI) ---